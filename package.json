{
  "name": "netlist-api",
  "version": "1.0.0",
  "description": "Backend API to run Netlist",
  "main": "index.js",
  "scripts": {
    "start": "babel-watch -w src src/index",
    "build": "rm -rf build && mkdir build && babel -d ./build ./src"
  },
  "repository": "git@github.com:treemmett/netlist-api.git",
  "author": "treemmett <tregan@tregan.me>",
  "license": "MIT",
  "dependencies": {
    "allow-methods": "^2.0.0",
    "babel-preset-env": "^1.7.0",
    "body-parser": "^1.18.3",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "ldapjs": "^1.0.2",
    "mongoose": "^5.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-watch": "^2.0.7"
  }
}
